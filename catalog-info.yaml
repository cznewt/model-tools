apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: model-tools
  title: Model Tools
  description: Container image with the desired-state renderers (kapitan, tanka, helm, kustomize, cue, timoni, kluctl) and secret helpers (sops, age, vals).
  tags:
    - gitops
  annotations:
    github.com/project-slug: cznewt/model-tools
spec:
  type: tool
  lifecycle: production
