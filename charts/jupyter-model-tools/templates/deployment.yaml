apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "jupyter-model-tools.fullname" . }}
  labels:
    {{- include "jupyter-model-tools.labels" . | nindent 4 }}
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "jupyter-model-tools.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "jupyter-model-tools.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: jupyter
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8888
              protocol: TCP
          {{- if or .Values.env .Values.existingSecret }}
          envFrom:
            - secretRef:
                name: {{ include "jupyter-model-tools.secretName" . }}
          {{- end }}
          {{- if .Values.persistence.enabled }}
          volumeMounts:
            - name: work
              mountPath: /source/work
          {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- if .Values.persistence.enabled }}
      volumes:
        - name: work
          persistentVolumeClaim:
            claimName: {{ include "jupyter-model-tools.fullname" . }}-work
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
