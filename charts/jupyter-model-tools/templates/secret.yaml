{{- if and (not .Values.existingSecret) .Values.env }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "jupyter-model-tools.fullname" . }}-env
  labels:
    {{- include "jupyter-model-tools.labels" . | nindent 4 }}
type: Opaque
stringData:
{{- range $name, $value := .Values.env }}
  {{ $name }}: {{ $value | quote }}
{{- end }}
{{- end }}
