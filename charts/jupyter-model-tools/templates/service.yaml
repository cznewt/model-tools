apiVersion: v1
kind: Service
metadata:
  name: {{ include "jupyter-model-tools.fullname" . }}
  labels:
    {{- include "jupyter-model-tools.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jupyter-model-tools.selectorLabels" . | nindent 4 }}
